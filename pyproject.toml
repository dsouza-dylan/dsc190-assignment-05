[project]
name = "dsc190-assignment-05"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.4",
]

[dependency-groups]
dev = [
    "pre-commit>=3.5.0",
    "ruff>=0.15.12",
]
